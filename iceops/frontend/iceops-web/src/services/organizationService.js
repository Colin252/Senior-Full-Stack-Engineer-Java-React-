import api from "../api/gateway"; const organizationService = { getAll: () => api.get("/organizations"), getById: (id) => api.get("/organizations/" + id), create: (organization) => api.post("/organizations", organization), update: (id, organization) => api.put("/organizations/" + id, organization), delete: (id) => api.delete("/organizations/" + id) }; export default organizationService;
