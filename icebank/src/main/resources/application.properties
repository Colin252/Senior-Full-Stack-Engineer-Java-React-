# -----------------------------
# SERVER
# -----------------------------
server.port=8091

# -----------------------------
# DATABASE (MySQL)
# -----------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/icebank_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=icebank_user
spring.datasource.password=IceBank123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# -----------------------------
# JPA
# -----------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# -----------------------------
# JWT
# -----------------------------
app.jwtSecret=IceBankSecretKey2025
app.jwtExpirationMs=604800000

# -----------------------------
# CORS
# -----------------------------
icebank.cors.allowed-origins=http://localhost:3000
